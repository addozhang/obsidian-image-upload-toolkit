# eslint-plugin-obsidianmd pins obsidian@1.8.7 exactly while obsidian-integration-testing
# requires ^1.12.3 — no version pair satisfies both peers. Ignore peer resolution until the
# eslint plugin relaxes its pin, matching CI's --legacy-peer-deps.
legacy-peer-deps=true
